/**
 * flashcards.js — Theory flashcards for ALL chapters.
 *
 * 📝 INSTRUCTIONS FOR THE AGENT:
 *
 * Populate this object with one top-level key per chapter (e.g. "topic1"),
 * then define its title, target page, and an array of card objects.
 *
 * Each card object must have:
 *   - "tag": short category/theme (e.g. "Paging")
 *   - "q": question text (in user's language)
 *   - "hint": brief hint/clue to guide the student
 *   - "a": detailed model answer (HTML allowed)
 *
 * Example:
 *
 * window.flashcardData = {
 *   "topic1": {
 *     "title": "Chapter 1 Title",
 *     "page": "topic1_basics.html",
 *     "cards": [
 *       { 
 *         "tag": "Basics", 
 *         "q": "What is X?",
 *         "hint": "Think about Y.",
 *         "a": "X is <strong>Y</strong> combined with Z." 
 *       }
 *     ]
 *   }
 * };
 */

window.flashcardData = {

    // ─── Add real chapter entries below ───────────────────────────────

};
