description = "Write per-section quiz questions for chapter N"

prompt = """
Execute the canonical workflow file workflows/quiz.md from the webnotes-builder
extension root (find it via /extensions list - typically
~/.gemini/extensions/webnotes-builder/workflows/quiz.md).

Read it fully and follow it exactly, substituting <N> = {{args}}.
"""
