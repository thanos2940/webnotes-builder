description = "Mine past exams into _build/exam_patterns.md (families, frequency matrix, pass-plan tiers)"

prompt = """
Execute the canonical workflow file workflows/mine.md from the webnotes-builder
extension root (find it via /extensions list - typically
~/.gemini/extensions/webnotes-builder/workflows/mine.md).

Read it fully and follow it exactly.
"""
