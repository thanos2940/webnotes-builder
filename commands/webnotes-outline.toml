description = "Outline chapter N - read the source, build outline + fidelity checklist"

prompt = """
Execute the canonical workflow file workflows/outline.md from the webnotes-builder
extension root (find it via /extensions list - typically
~/.gemini/extensions/webnotes-builder/workflows/outline.md).

Read it fully and follow it exactly, substituting <N> = {{args}}.
"""
