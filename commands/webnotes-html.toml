description = "Write the HTML page for chapter N from its outline"

prompt = """
Execute the canonical workflow file workflows/html.md from the webnotes-builder
extension root (find it via /extensions list - typically
~/.gemini/extensions/webnotes-builder/workflows/html.md).

Read it fully and follow it exactly, substituting <N> = {{args}}.
"""
