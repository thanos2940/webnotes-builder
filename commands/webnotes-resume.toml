description = "Resume the webnotes build from _build/STATE.md"

prompt = """
Execute the canonical workflow file workflows/resume.md from the webnotes-builder
extension root (find it via /extensions list - typically
~/.gemini/extensions/webnotes-builder/workflows/resume.md).

Read it fully and follow it exactly.
"""
